- name: install and run nginx
  hosts: web
  become: yes # equal to -b in adhoc commands, getting root acess
  tasks: 
  - name: install nginx
    ansible.builtin.dnf:
    state: latest
  - name: start nginix
    state: started
    enable: yes

